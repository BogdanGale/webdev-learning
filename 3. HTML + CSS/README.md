## Synopsis

Web pages that contain a little bit of CSS styling

## Technologies used

- CSS selectors (tag, nth-of-type)
- CSS styling (font-family, padding, letter-spacing, font-weight, font-size, text-transform, width, float, borders, margins, etc.)
